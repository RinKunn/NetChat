<Window x:Class="NetChat.Desktop.View.Messenger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NetChat.Desktop.ViewModel"
        xmlns:views="clr-namespace:NetChat.Desktop.View.Messenger"
        xmlns:behaviors="clr-namespace:NetChat.Desktop.View.Behaviors"
        xmlns:mvm="clr-namespace:NetChat.Desktop.ViewModel.Messenger"
        xmlns:local="clr-namespace:NetChat.Desktop"
        mc:Ignorable="d"
        Style="{StaticResource CustomWindowStyle}"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}"
        Title="MainWindow" 
        d:DesignHeight="350" d:DesignWidth="350"
        Height="450" Width="400"
        MinWidth="380" MinHeight="350">
    
    <i:Interaction.Behaviors>
        <behaviors:ActivateBehavior Activated="{Binding IsVisible, Mode=TwoWay}"/>
    </i:Interaction.Behaviors>
    
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Deactivated">
            <i:InvokeCommandAction Command="{Binding DeactivatedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Window.Resources>
        <DataTemplate DataType="{x:Type mvm:HeaderViewModel}">
            <views:HeaderView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type mvm:ChatAreaViewModel}">
            <views:ChatAreaView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type mvm:ChatSenderViewModel}">
            <views:ChatSenderView/>
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <ContentControl Content="{Binding Header}" Grid.Row="0" Panel.ZIndex="1"/>
        <ContentControl Content="{Binding ChatArea}" Grid.Row="1"/>
        <ContentControl Content="{Binding ChatSender}" Grid.Row="2" Height="Auto"/>
        <ContentControl Content="{Binding SideArea}" Grid.Column="1"/>
    </Grid>
</Window>
