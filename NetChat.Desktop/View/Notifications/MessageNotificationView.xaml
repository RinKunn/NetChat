<UserControl x:Class="NetChat.Desktop.View.Notifications.MessageNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NetChat.Desktop.ViewModel.Notifications" 
             xmlns:local="clr-namespace:NetChat.Desktop.View.Notifications"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:MessageNotification, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="FontFamily" Value="Arial"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="Red">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding Sender}"/>
        <TextBlock Text="sdfsdfsdfsdfsdf"
                   TextWrapping="Wrap"
                   TextTrimming="CharacterEllipsis"
                   Grid.Row="1"/>
    </Grid>
</UserControl>
