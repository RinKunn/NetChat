<UserControl x:Class="NetChat.Desktop.View.Messenger.ChatArea.Messages.TextMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:NetChat.Desktop.View.Messenger.ChatArea.Messages"
             xmlns:viewmodels="clr-namespace:NetChat.Desktop.ViewModel.Messenger.ChatArea.Messages"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:TextMessageObservable, IsDesignTimeCreatable=False}"
             d:DesignHeight="70" 
             d:DesignWidth="320">
    
    <views:MessageView>
        <TextBlock
            Text="{Binding Text}"
            Foreground="{DynamicResource WhiteBrush}"
            TextWrapping="Wrap" 
            VerticalAlignment="Stretch" 
            HorizontalAlignment="Stretch"/>
    </views:MessageView>
</UserControl>
