<UserControl x:Class="NetChat.Desktop.View.Notificator.ParticipantNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NetChat.Desktop.ViewModel.Notifications" 
             xmlns:local="clr-namespace:NetChat.Desktop.View.Notificator"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="200"
             d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type=vm:ParticipantNotificationItem}"
             Template="{DynamicResource NotificationViewTemplate}" Height="80" Width="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="./NotificationItemView.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.RenderTransform>
        <ScaleTransform/>
    </UserControl.RenderTransform>
    
    <Grid>
        <TextBlock
            Text="{Binding Message}"
            Style="{StaticResource NotificationMessageStyle}"
            TextWrapping="Wrap"
            TextTrimming="CharacterEllipsis">
        </TextBlock>
    </Grid>
</UserControl>
